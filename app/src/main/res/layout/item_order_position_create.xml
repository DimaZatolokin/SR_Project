<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="model"
            type="com.srproject.presentation.models.OrderPositionUI" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/imgProgress"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:contentDescription="@string/position_progress"
            android:scaleType="centerCrop"
            app:positionDone="@{model.done}" />

        <Spinner
            android:id="@+id/spProducts"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="3"
            app:selectedPositionItem="@{model.product.name}"
            tools:text="Коробка конфет" />

        <com.srproject.presentation.customViews.CustomAmountPicker
            android:id="@+id/amountPicker"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </LinearLayout>
</layout>